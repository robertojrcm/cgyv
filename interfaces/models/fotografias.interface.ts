export interface FotografiasAtributos {
    id               ?: number;
    gasto_id          ?: number;
    ruta_archivo      ?: string;
    fecha_creacion     ?: Date;
    esta_activo       ?: boolean;
}